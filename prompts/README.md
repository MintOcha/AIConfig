# Prompt catalog

`coding-rules.md` is one selectable prompt. Add additional Markdown prompt
files in this directory, then describe them in `prompts.toml` so the setup menu
can show a useful label and description.
