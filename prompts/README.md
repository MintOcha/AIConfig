# Prompt catalog

`shared.md` is the shared default. Add additional Markdown prompt files in this
directory, then describe them in `prompts.toml` so the setup menu can show a
useful label and description.
