# Portable MCP catalog for the setup menu.
#
# Keep credentials out of this file. For remote MCPs, use a {PLACEHOLDER} in
# url. The setup menu asks for its value only when that MCP is selected.

[mcp.duckduckgo]
label = "DuckDuckGo search"
category = "search"
description = "Free web search and page fetching; no API key required"
transport = "stdio"
command = "uvx"
args = ["--refresh", "--from", "duckduckgo-mcp-server[browser]", "duckduckgo-mcp-server", "--search-backend", "curl"]

[mcp.tavily]
label = "Tavily search"
category = "search"
description = "Web search and research through Tavily"
transport = "url"
url = "https://mcp.tavily.com/mcp/?tavilyApiKey={TAVILY_API_KEY}"
secret_label = "Tavily API key"
