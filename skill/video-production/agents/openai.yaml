interface:
  display_name: "Video Production"
  short_description: "Prepare, subtitle, design, and compress video"
  default_prompt: "Use $design to establish the visual direction, then use $video-production to build, subtitle, inspect, and deliver the video."
